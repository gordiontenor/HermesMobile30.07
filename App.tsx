// App.tsx — fallback entry point.
// expo-router/entry is now the main entry (see package.json "main").
// This file is retained for any legacy references to <App />.
export default function App() {
  return null;
}
